Problem Statement:
   Build a file based key-value data store that support the basic 
     ->create
     ->read
     ->delete
  operations, the data stored must be locally stored in the system.
 
 
 SOURCE CODE
  
IMPORT         

import json    # for file saving and uploading      
import time

DATABASE

The database is stored locally in json format i have taken 2 databases,one is master database and the other is temporary database temporary data will store all the 
operations for one single process.

#master={}    #Do onlyone title
temp={}
x=input("Do you want to import master database and perform operations on it yes/no:')
if(x=="yes"):
#Load master database 
print('loading master database in temp....')
with open('master.json','r')as masteropen:
data_load = json.load(masteropen)
temp=data_load
print('loaded master database')
print(temp)

Do you want to import master database and perform operation on it yes/no:

FUNCTIONS

d=temp
def read(k):
if k not in d:
print("Error:this key is not present")
else:
b=d[k]
if time.time()<b[1]:
stri=str(k)+":"+str(b[0])
return stri
else:
print("Error: time-to-live",k,"has expired")
else:
if(x==4):
break
if(x==1):
key=input('enter key for input')
value=int(input('enter its corresponding value'))
#timeout=int(input('enter time,default is 0'))
create(key,value)#timeout)
if(x==2):
key=input('enter key to read')
print(read(str(key)))
if(x==3):
key=input('enter key')
delete(key)
print('key deleted')
if(x==5):
print(d)

Enter 1 for create ,2 for read,3 for delete,4 to exit,5 to show data 1
Enter key for input1
Enter its corresponding value 2


Enter 1 for create ,2 for read,3 for delete,4 to exit,5 to show data 1
Enter key for input 3
Enter its corresponding value 4


Enter 1 for create ,2 for read,3 for delete,4 to exit,5 to show data 4

SAVING THE DATA

import json
temp=d
with open('temp.json','w') as fp:
json.dump(temp,fp)

print('your database after operation yes/no')
print(temp)
if(x=="yes");
with open('master.json','w')as fp:
json.dump(temp,fp)
print("thankyou")
exit()
is the first ever operation yes/no: yes

thankyou

x=input(Do you want to save this is the master dataset yes/no:)
if(x=='yes');
data={}
import json
with open('master.json','r') as fp:
data=json.dump(master,fp)
print('All task done ,thanks')

Do you want to save this is the master dataset yes/no:yes
All task done,thanks
